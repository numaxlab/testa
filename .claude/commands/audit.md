Your goal is to update vulnerable dependencies.

Do the following:

1. Run `composer audit` to find vulnerable installed packages in this project
2. Run `composer audit fix` to apply updates
3. Run tests and verify the updates didn't break anything